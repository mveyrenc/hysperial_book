- if attribute_body_renderer.render?
  .field.is-horizontal
    - if attribute_label_renderer.render?
      .field-label
        .label
          = render attribute_label_renderer
    .field-body
      = render attribute_body_renderer
