= render Layout::MainArea::Component.new(Bookcase::ContentTagFamily) do |component|
  - component.with_title do
    = t('.title')
  - component.with_actions do
    = render Bookcase::ContentTagFamilies::Components::ActionButtons::Component.new(actions: %i[new])
  - component.with_left_aside do
    = render Bookcase::ContentTagFamilies::Components::SearchForm::Component.new(params: params, aggs: @records.aggs)
  - component.with_upper_section do
    = turbo_frame_tag Bookcase::ContentTagFamily.new
  - component.with_main_section do
    .container
      - if @records.records.any?
        - @records.records.each do |record|
          = turbo_frame_tag record.object do
            = render Bookcase::ContentTagFamilies::Components::Card::Component.new(record)
        = paginate @records.records
      - else
        = render NoRecord::Component.new
