.box
  h1.title
    = t('.title')
  - if @books.list.blank?
    = t('application.no_entry')
  - else
    .columns.is-multiline
      - @books.list.each do |book|
        .column
          = render Books::Components::CardComponent.new(book: book)
