.card-box
  = form_with(url: search_content_tags_path, builder: AppFormBuilder::FormBuilder, method: :get, data: { turbo: "false" }) do |f|
    = f.text_field :q, value: params_q, hide_label: true, placeholder: t('application.search_q_placeholder')
    = f.select :content_tag_family_kind, options_for_select(content_tag_family_kind_select_values, params[:content_tag_family_kind]), include_blank: true
    = f.select :content_tag_family_name, options_for_select(content_tag_family_name_select_values, params[:content_tag_family_name]), include_blank: true
    = f.select :book_kind, options_for_select(book_kind_select_values, params[:book_kind]), include_blank: true
    = f.select :book_name, options_for_select(book_name_select_values, params[:book_name]), include_blank: true
    .field
      .buttons
        = f.submit t('application.search'), class: 'button is-primary'
        = link_to t('application.reset search'), content_tags_path, class: 'button'
