= render Layout::MainArea::Component.new do |component|
  - component.with_title do
    = t('.title')
  - component.with_actions do
    - if policy(ContentTag).create?
      = turbo_frame_tag ContentTag.new do
        = render ContentTags::Components::NewButton::Component.new
  - component.with_left_aside do
    = render ContentTags::Components::SearchForm::Component.new(params: params, aggs: @records.aggs)
  - component.with_main_section do
    = turbo_frame_tag "content_tags", class: "container" do
      - if @records.records.any?
        - @records.records.with_highlights.each do |record, highlights|
          = turbo_frame_tag record do
            = render ContentTags::Components::Card::Component.new(record: record, highlights: highlights)
      - else
        = render NoRecord::Component.new
    = paginate @records.records
